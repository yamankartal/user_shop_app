
enum States{init,loading,loaded,error}
enum FromScreen{pending,archive, cart, products, orders, archivedOrders,address, forgetPassword, signUp}
enum Screen{pending,archive,other, orders, tracking,orderDetails}

enum Auth{userName, phone, email, confirmPassword, password}